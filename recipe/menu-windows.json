{
    "menu_name": "${DISTRIBUTION_NAME}",
    "menu_items":
    [
        {
            "name": "Prismatic",
            "script": "${PREFIX}/Library/bin/prismatic-gui.exe",
            "icon": "${MENU_DIR}/prismatic.ico"
        }
    ]
}
